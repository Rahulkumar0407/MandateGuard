# Plan: Fix M8-A Deployment (incomplete commit)

## Context
M8-A (interactive merchant demo) was implemented but never fully committed. The
pushed tip `origin/main` is `bf48f71` (`feat: harden interactive merchant demo`),
which contains only the *hardening delta*:

- `app/api/demo/mutate-offer/route.ts`
- `components/DemoConsole.tsx`
- `tests/app/demo-console.test.ts`

The two pieces that actually *wire the demo into the app* were left in the working
tree and are NOT in the deployed build:

- `app/page.tsx` — still `M` (uncommitted). Pushed `HEAD:app/page.tsx` is the
  minimal M0 landing page; it does **not** import `DemoConsole`.
- `app/api/demo/seed/` — untracked directory; the seed route is absent from the
  deployed build.

Because `DemoConsole` POSTs to `/api/demo/seed` on every intent search, the
deployed app breaks at runtime (404 on seed). The expected
`feat: complete interactive demo wiring` commit was never created. This is the
deployment failure.

Verified facts:
- `git ls-remote origin` → `origin/main = bf48f71` (hardening commit IS pushed).
- `git show HEAD:app/page.tsx` → minimal M0 page, no `DemoConsole` import.
- `git status --short` (last observed) → ` M AGENTS.md`, ` M app/page.tsx`,
  `?? app/api/demo/seed/`, `?? prisma/migrations/`.

## Root cause
Partial commit of M8-A: page wiring + seed route never committed/pushed.

## Fix (implementation steps for the exec agent)

1. **Enumerate outstanding M8-A files** (read-only):
   `git status --short` and confirm the only M8-A-related uncommitted paths are
   `app/page.tsx` and everything under `app/api/demo/seed/`.
   Also confirm `lib/merchant/seed-data.ts` is already tracked (it must be, since
   it is not listed as untracked). If any *other* M8-A file is uncommitted,
   include it in the next commit too.

2. **Stage ONLY the M8-A wiring files** (do NOT stage `AGENTS.md` or
   `prisma/migrations/`):
   - `app/page.tsx`
   - `app/api/demo/seed/route.ts` (and any sibling files in `app/api/demo/seed/`)

3. **Commit** as:
   `feat: complete interactive demo wiring`

4. **Verify the complete tree builds** (this is the safeguard that catches any
   real compile/lint error, which we could not see without Vercel logs):
   - `npm run typecheck`
   - `npm run lint`
   - `npm test` (full suite; expect 329+ passing)
   - `npm run build`  ← must succeed before deploy

5. **Push both commits** to `origin/main`:
   - NOTE: in the prior session `git push origin main` failed with
     "could not read Username for 'https://github.com'" — there are no GitHub
     credentials in this environment. The push must be done from an
     authenticated context (token / `gh` / credential helper). Flag this as a
     hard dependency: deployment cannot happen until the repo is pushed.
   - Do NOT use interactive Vercel login.

6. **Vercel deploy** (GitHub → Vercel git integration auto-deploys on push):
   - Wait for the build; confirm status = **Ready**.
   - Confirm production URL `https://getmandateguard.vercel.app` serves the new
     commit.

7. **Read-only production verification** (no mutations):
   - `GET /api/demo/seed` → expect 405 (route exists, POST-only).
   - `GET /api/demo/mutate-offer` → expect 405.
   - `GET /` → HTML should now mount `DemoConsole` (look for "MandateGuard
     Console" / "Append-only Audit Timeline" markers).
   - Do NOT POST to either demo route yet (defer to M8-B).

8. **Protected baseline unchanged** (read-only Supabase):
   - `Subscription` where `razorpaySubscriptionId='sub_TTtTlxiMx6j10A'` →
     status `PENDING`.
   - `WebhookEvent` count for `cmt89wxmb0000lp7yrdkuyu1u` → `0`.
   - No provider actions, no `LIVE_ACTIONS_ENABLED`, no subscription changes.

## Excluded from this fix
- `AGENTS.md` (unrelated doc change) — leave uncommitted.
- `prisma/migrations/` (untracked) — leave untracked; not part of M8-A and not
  required for the app build.
- No M0–M7 logic, ActionExecutor, policy, integrity, semantic, Razorpay gateway,
  webhooks, or audit schema changes.

## Validation / Definition of Done
- Both `feat: complete interactive demo wiring` and the already-pushed
  `feat: harden interactive merchant demo` are present on `origin/main`.
- `npm run build` passes locally.
- Deployed app shows `DemoConsole`; `/api/demo/seed` and `/api/demo/mutate-offer`
  routes exist (405 on GET).
- Protected subscription still `PENDING`, WebhookEvent count `0`.
- No provider mutation performed.

## Open questions / risks
- Actual Vercel build error text is unknown (no Vercel/auth access). The
  `npm run build` step in the fix is the control that guarantees a clean build
  before deploy.
- Push requires GitHub authentication not present in this sandbox; arrange an
  authenticated push before expecting a deploy.
