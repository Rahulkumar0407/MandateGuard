// No-op stub for `server-only` used only in the vitest environment.
// In the real Next.js server build the package enforces server-only usage;
// under vitest we just need it to not throw.
export {};
